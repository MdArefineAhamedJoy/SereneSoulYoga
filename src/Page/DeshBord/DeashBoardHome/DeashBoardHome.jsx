import React from "react";
import useUsers from "../../../Hooks/useUsers";

const DeashBoardHome = () => {
  const [allUser] = useUsers();
//   const userRoll = allUser.map((roll) => console.log(roll?.role));
  return <div></div>;
};

export default DeashBoardHome;
